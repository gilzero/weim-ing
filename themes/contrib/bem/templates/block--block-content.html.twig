{% extends "block.html.twig" %}

{% block content %}
  {%
    set sub_bem_block = 'block-content-' ~ block_content_bundle|clean_class ~ '-' ~ configuration.view_mode|clean_class
  %}
  <div class="{{ bem_block }}__content {{ sub_bem_block }}">
    {{ content }}
  </div>
{% endblock %}
